package acm;

public class MyClassClass {
    /**
     * Multiplies two numbers together without using * or / operators
     * @param x one multiplicand
     * @param y one multiplicand
     * @return the product of x and y
     */
    public static int multiply(int x, int y) {
        return -1;
    }

    /**
     * Determines the position in the array of the element
     * @param array an array of elements
     * @param element an item that may be contained in the array
     * @return the index in the array of the element, -1 if it does not contain the element.
     */
    public static <T> int indexOf(T[] array, T element) {
        return -1;
    }

    /**
     * Determines the correct thing to print for the given number during the game of FizzBuzz. During FizzBuzz, you say
     * Fizz if a number is divisible by 3, Buzz if it is divisible by 5, and FizzBuzz if it is divisible by both 5 and
     * 3. If it is not divisible by 5 or 3, just say the number.
     * @param numberToCheck the number to check the fizzBuzziness of
     * @return A string "Fizz", "Buzz", "FizzBuzz", or the number passed as numberToCheck
     */
    public static String fizzBuzz(int numberToCheck) {
        return "Fizz";
    }
}
